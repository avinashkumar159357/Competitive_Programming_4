#include<bits/stdc++.h>
#include<cmath>
using namespace std;

void solve(){
    int n,m;
    cin>>n>>m;
    cout<<(n/3)*(m/3)<<"\n"; //one sonar beam cover 3 row grid
                            //and 3 column grid so divide each by 3 and then multiply
                            
}

int main() {
    ios :: sync_with_stdio(0);
    cin.tie(0);
    
    int t;
    cin>>t;
    while(t--) {
        solve();
    }

}    
