#include<bits/stdc++.h>
#include<cmath>
using namespace std;

void solve(){
    string line;
    while (getline(cin,line))  //getting the inpuut this way 
        cout<<line<<"\n";      //throghg getline() output the spaces
   }                           //as it is unlike directly cin which removes all the whitespacs 
                               //in between .



int main() {
    ios :: sync_with_stdio(0);
    cin.tie(0);
    
    int t=1;
    while(t--) {
        solve();
    }

}    
